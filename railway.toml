[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m outfindr.worker"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[mounts]]
source = "outfindr-data"
destination = "/data"
